#!/usr/bin/env python3
from __future__ import annotations

import os
import sys

lib_path = os.path.join(os.path.dirname(os.path.dirname(os.path.abspath(__file__))), 'lib')
sys.path.insert(0, lib_path)

from sweer_config import ClientConfig
from sweer_utils import _print_response_with_metadata, send_request

config = ClientConfig()

def close():
    """Close the currently open window."""
    response = send_request(config.port, "close", "POST")
    if response is None:
        return
    _print_response_with_metadata(response)

if __name__ == "__main__":
    close()